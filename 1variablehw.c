#include <stdio.h>

int main() {
    printf("enter the length of RECTANGLE\n");
    float length;
    scanf("%f", &length);
    printf("enter the breadth of RECTANGLE\n");
    float breadth;
    scanf("%f", &breadth);
    printf("area of RECTANGLE:%f\n", length*breadth);//area of ractangle
    printf("enter the side of CUBE\n");
    float side;
    scanf("%f", &side);
    printf("volume of CUBE%f\n", side*side*side);/*volume of 
    cube*/
    return 0;
}